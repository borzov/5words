# WordleHelper 2.0

WordleHelper - это продвинутый помощник для игры "5 слов" (русский аналог Wordle), который поможет вам найти правильное слово используя различные стратегии и аналитику.

## 🚀 Новые возможности версии 2.0

- **🎯 Интерактивный режим** с пошаговыми подсказками
- **📊 Сортировка по частотности** русских букв  
- **🎲 Предложения оптимальных слов** для проверки
- **📈 Статистика и аналитика** по буквам
- **⚡ Улучшенная валидация** и обработка ошибок
- **🔢 Корректная обработка** повторяющихся букв

## Описание

Скрипт фильтрует слова из словаря русских слов (50k+ слов) на основе критериев:
- ✅ **Известные буквы** на определенных позициях
- ❓ **Неизвестные буквы**, которые точно есть в слове
- ❌ **Исключенные буквы**, которых точно нет в слове

## 🛠 Установка и запуск

```bash
# Клонируйте репозиторий или скачайте файлы
# Убедитесь что у вас есть Python 3.6+

# Обычное использование
python3 main.py --known "м_тр_" --unknown "о" --excluded "узк"

# Интерактивный режим (рекомендуется!)
python3 main.py --interactive
```

## 📖 Режимы использования

### 🎯 Интерактивный режим (рекомендуется)

Самый удобный способ решать головоломки:

```bash
python3 main.py --interactive
```

**Как это работает:**
1. Программа предлагает оптимальное слово для проверки
2. Вы вводите слово и результат из игры
3. Программа автоматически обновляет критерии поиска
4. Процесс повторяется до нахождения ответа

**Формат ввода результата:**
- `+` — буква на правильной позиции (зеленая)
- `?` — буква есть в слове, но не на этой позиции (желтая)  
- `-` — буквы нет в слове (серая)

**Пример сессии:**
```
💡 Рекомендую попробовать: 'АДРЕС'
Введите слово и результат: адрес ?а+д-р-е?с

📊 Попытка 2
Найдено 15 слов: садик, кадык, садок...
💡 Рекомендую попробовать: 'САДИК'
```

### 🔧 Командный режим

Для точечных запросов и автоматизации:

```bash
# Базовый поиск
python3 main.py --known "м_тр_" --unknown "о" --excluded "узк"

# С дополнительными опциями
python3 main.py --known "_а___" --stats --suggest --limit 10
```

## 📋 Параметры командной строки

| Параметр | Описание | Пример |
|----------|----------|---------|
| `--known` | Буквы на известных позициях (используйте `_` для неизвестных) | `м_тр_` |
| `--unknown` | Буквы, которые есть в слове, но позиция неизвестна | `оа` |
| `--excluded` | Буквы, которых точно нет в слове | `узк` |
| `--limit N` | Ограничить количество результатов | `--limit 10` |
| `--no-sort` | Отключить сортировку по частотности | |
| `--stats` | Показать статистику по буквам | |
| `--suggest` | Предложить оптимальное следующее слово | |
| `--interactive` | Запустить интерактивный режим | |

## 💡 Примеры использования

### Базовые примеры

```bash
# Известны 1-я и 3-я буквы
python3 main.py --known "м_т__"

# Знаем что есть буквы "о" и "а", но не знаем где
python3 main.py --unknown "оа"

# Исключаем часто встречающиеся буквы
python3 main.py --excluded "стлн"

# Комбинированный поиск
python3 main.py --known "м_тр_" --unknown "о" --excluded "узк"
```

### Продвинутые примеры

```bash
# Поиск с анализом и предложениями
python3 main.py --known "_а___" --stats --suggest

# Ограниченный вывод без сортировки (как в оригинальном словаре)
python3 main.py --unknown "оо" --limit 5 --no-sort

# Получить предложение для начала игры
python3 main.py --suggest
```

## 🧠 Алгоритм и стратегия

### Сортировка по частотности
Результаты автоматически сортируются по частотности букв в русском языке:
- Приоритет отдается словам с буквами: `о, е, а, и, н, т, с, р`
- Это увеличивает вероятность найти правильный ответ быстрее

### Предложения слов
Алгоритм анализирует оставшиеся варианты и предлагает слова, которые:
- Максимально разделят множество возможных ответов
- Содержат наиболее информативные буквы
- Помогут исключить больше вариантов за один ход

### Обработка повторяющихся букв
Корректно обрабатывает случаи с повторяющимися буквами:
```bash
# Ищет слова с двумя буквами "о"
python3 main.py --unknown "оо"
```

## 📊 Статистика и аналитика

```bash
# Получить статистику по буквам
python3 main.py --known "_а___" --stats
```

**Выводит:**
- Частоту встречаемости букв в найденных словах
- Статистику фильтрации на каждом этапе
- Рекомендации по стратегии

## ⚠️ Обработка ошибок

Программа включает продвинутую валидацию:
- ✅ Проверка длины параметров (должно быть 5 символов)
- ✅ Проверка валидности символов (только русские буквы)
- ✅ Обнаружение конфликтов между `known` и `unknown`
- ✅ Информативные сообщения об ошибках

## 🎯 Советы по стратегии

1. **Начинайте с интерактивного режима** - он покажет оптимальную стратегию
2. **Используйте стартовые слова** с частыми буквами: АДРЕС, СТЕНА, ТОСКА
3. **Анализируйте статистику** чтобы понять какие буквы искать
4. **Не игнорируйте предложения** - алгоритм учитывает теорию информации

## 🔧 Технические детали

- **Словарь:** 50,000+ русских слов длиной 5 букв
- **Алгоритм:** Regex-фильтрация + эвристическая оптимизация
- **Производительность:** Обработка словаря за ~0.1 сек
- **Память:** ~5MB для загрузки словаря

## 📈 История версий

### Версия 2.0.0
- ➕ Интерактивный режим с ИИ-подсказками
- ➕ Сортировка по частотности русских букв
- ➕ Предложения оптимальных слов
- ➕ Статистика и аналитика
- 🔧 Исправлена логика обработки повторяющихся букв
- 🔧 Улучшена валидация и обработка ошибок

### Версия 1.1.0
- 🔧 Базовая валидация
- 🔧 Обработка ошибок файловой системы

### Версия 1.0.3
- 🚀 Первоначальная версия

## 👨‍💻 Автор

**Maksim Borzov**

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.